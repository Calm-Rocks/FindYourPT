# Dependencies — always reinstalled from package.json, never committed
node_modules/

# Build output
dist/
dist-ssr/

# Local env files (contain your real Supabase credentials — never commit these)
.env
.env.local
.env.*.local

# Editor/OS noise
.DS_Store
*.local
.vscode/*
!.vscode/extensions.json

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
